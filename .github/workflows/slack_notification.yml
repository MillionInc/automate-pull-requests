name: Slack Notification

on:
  issue_comment:
    types: [created, edited]
jobs:
  notify_slack:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          ref: ${{ github.event.pull_request.head.ref }}

      - name: Notify Slack
        if: github.event.issue.pull_request
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
        run: |
          curl -X POST -H 'Content-type: application/json' \
            --data '{"text": "A comment was ${{ github.event.issue_comment.action }} on Pull Request #${{ github.event.issue.number }} by ${{ github.event.comment.user.login }}. The event content is: ```\n${{ github.event.issue }}\n```"}' $SLACK_WEBHOOK_URL
